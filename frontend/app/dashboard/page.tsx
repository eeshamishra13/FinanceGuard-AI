"use client";
import { useEffect } from "react";
import { Area, AreaChart, Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { SiteNav } from "@/components/site-nav";
import { AnimatedNumber, GlassCard, MetricLabel, PrimaryButton, ResilienceRing, RunwayGauge, SectionHeading } from "@/components/ui/finance";
import { mockCashFlow, mockForecast, mockMetrics, mockProfile } from "@/data/mockData";

const scoreLabels: [string, keyof typeof mockMetrics.resilienceBreakdown][] = [["Emergency fund", "emergencyFund"], ["Savings rate", "savingsRate"], ["Debt burden", "debtBurden"], ["Expense stability", "expenseStability"], ["Income stability", "incomeStability"]];
const money = (value: number) => `₹${value.toLocaleString("en-IN")}`;

export default function Dashboard() { useEffect(() => {
  const handleScroll = () => {
    const scrollY = window.scrollY;
    const rotation = scrollY * 0.02; // adjust multiplier for subtlety
    document.querySelectorAll(".glass-card").forEach((card, i) => {
      (card as HTMLElement).style.transform = `rotate(${rotation + i * 3}deg)`;
    });
  };
  window.addEventListener("scroll", handleScroll);
  return () => window.removeEventListener("scroll", handleScroll);
}, []);

  return <main className="dashboard"><SiteNav /><div className="dashboard-head"><div><MetricLabel>FINANCIAL TWIN — LIVE STATE</MetricLabel><h1>Your financial command center.</h1></div><span className="active-status"><i /> MODEL ACTIVE</span></div><section className="metric-strip">{[["NET WORTH", mockMetrics.netWorth], ["MONTHLY INCOME", mockProfile.income], ["MONTHLY EXPENSES", mockMetrics.totalExpenses], ["SAVINGS RATE", mockMetrics.savingsRate]].map(([label, value], i) => <div className="top-metric" key={label as string}><MetricLabel>{label as string}</MetricLabel><strong><AnimatedNumber value={value as number} prefix={i === 3 ? "" : "₹"} suffix={i === 3 ? "%" : ""} decimals={i === 3 ? 1 : 0} /></strong><span className="metric-delta">{i === 0 ? "+12.4% this quarter" : i === 3 ? "↑ above target" : "vs. last month"}</span></div>)}</section><section className="dashboard-grid top-grid"><GlassCard className="net-worth-card"><SectionHeading eyebrow="NET WORTH / TRAJECTORY" title="A stronger position, month by month." detail="Your assets are outpacing your obligations." /><div className="net-worth-total"><AnimatedNumber value={mockMetrics.netWorth} prefix="₹" /><span>+ ₹23,000 projected monthly</span></div><div className="mini-line">{mockForecast.map((point, i) => <span key={point.month} style={{ height: `${28 + (point.netWorth - 600000) / 3600}px` }} className={i === 0 ? "muted-bar" : ""} />)}</div></GlassCard><GlassCard className="resilience-card"><SectionHeading eyebrow="RESILIENCE / 100" title="How much can you absorb?" /><div className="resilience-layout"><ResilienceRing score={mockMetrics.resilienceScore} band={mockMetrics.resilienceBand} /><div className="score-bars">{scoreLabels.map(([label, key]) => <div className="score-row" key={key}><div><span>{label}</span><b>{mockMetrics.resilienceBreakdown[key]}</b></div><div className="score-track"><i style={{ width: `${mockMetrics.resilienceBreakdown[key] / 28 * 100}%` }} /></div></div>)}</div></div></GlassCard></section><section className="dashboard-grid lower-grid"><GlassCard className="cash-card"><SectionHeading eyebrow="CASH FLOW / MONTHLY" title="The rhythm of your money." /><div className="cash-stats"><div><MetricLabel>MONTHLY SAVINGS</MetricLabel><strong><AnimatedNumber value={mockMetrics.monthlySavings} prefix="₹" /></strong></div><div><MetricLabel>MONTHLY BURN</MetricLabel><strong><AnimatedNumber value={mockMetrics.monthlyBurn} prefix="₹" /></strong></div></div><ResponsiveContainer width="100%" height={190}><BarChart data={mockCashFlow} barGap={5}><CartesianGrid vertical={false} stroke="var(--line)" /><XAxis dataKey="month" tick={{ fill: "var(--muted-text)", fontSize: 10 }} axisLine={false} tickLine={false} /><YAxis hide /><Tooltip cursor={{ fill: "var(--surface)" }} contentStyle={{ background: "var(--panel)", border: "1px solid var(--line)", color: "var(--foreground)" }} formatter={(v) => money(Number(v))} /><Bar dataKey="income" fill="var(--accent)" radius={[2, 2, 0, 0]} /><Bar dataKey="expenses" fill="var(--slate)" radius={[2, 2, 0, 0]} /></BarChart></ResponsiveContainer><div className="chart-key"><span><i className="key-income" /> INCOME</span><span><i className="key-expense" /> EXPENSES</span></div></GlassCard><GlassCard className="runway-card"><SectionHeading eyebrow="RUNWAY / LIQUIDITY" title="Room to keep moving." detail="At your current monthly burn." /><RunwayGauge months={mockMetrics.runwayMonths} /><div className="runway-foot"><span>Emergency fund <b>{money(mockProfile.emergencyFund)}</b></span><span>Safety threshold <b>6.0 mo</b></span></div></GlassCard></section><GlassCard className="forecast-card"><SectionHeading eyebrow="FORECAST / NEXT 6 MONTHS" title="The shape of your current trajectory." detail="Projected net worth, modeled from your live state." /><ResponsiveContainer width="100%" height={270}><AreaChart data={mockForecast}><defs><linearGradient id="emeraldFill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="var(--accent)" stopOpacity={0.25} /><stop offset="100%" stopColor="var(--accent)" stopOpacity={0} /></linearGradient></defs><CartesianGrid stroke="var(--line)" vertical={false} /><XAxis dataKey="month" tickFormatter={(v) => `M${v}`} tick={{ fill: "var(--muted-text)", fontSize: 10 }} axisLine={false} tickLine={false} /><YAxis tickFormatter={(v) => `₹${v / 100000}L`} tick={{ fill: "var(--muted-text)", fontSize: 10 }} axisLine={false} tickLine={false} width={40} /><Tooltip contentStyle={{ background: "var(--panel)", border: "1px solid var(--line)", color: "var(--foreground)" }} labelFormatter={(v) => `Month ${v}`} formatter={(v) => [money(Number(v)), "Net worth"]} /><Area type="monotone" dataKey="netWorth" stroke="var(--accent)" strokeWidth={2} fill="url(#emeraldFill)" dot={{ fill: "var(--accent)", r: 3, strokeWidth: 0 }} /></AreaChart></ResponsiveContainer></GlassCard><section className="stress-cta"><div><MetricLabel>SCENARIO ENGINE / READY</MetricLabel><h2>What if everything changed?</h2><p>Test the variables that feel impossible to model alone.</p></div><div className="stress-list"><span>JOB LOSS</span><span>RENT INCREASE</span><span>UNEXPECTED EXPENSE</span><span>INCOME CHANGE</span></div><PrimaryButton href="/simulator">RUN A STRESS TEST</PrimaryButton></section></main>; }
